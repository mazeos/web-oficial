services:
  web-oficial:
    build: .
    restart: unless-stopped
    networks:
      - traefik-public
    labels:
      - traefik.enable=true
      - "traefik.http.routers.web-oficial.rule=Host(`go.mazefunnels.io`)"
      - traefik.http.routers.web-oficial.entrypoints=websecure
      - traefik.http.routers.web-oficial.tls.certresolver=letsencrypt
      - traefik.http.services.web-oficial.loadbalancer.server.port=80
networks:
  traefik-public:
    external: true
